# Operator2 Node Deployment Environment
# These variables are specific to this deployment instance

# EdgeLake init type (prod: run EdgeLake, bash: shell interface)
INIT_TYPE=prod

# Docker Image Configuration
IMAGE=anylogco/edgelake
TAG=latest


# Remote CLI params (if REMOTE_CLI=true in advance_configs.env)
CONN_IP=0.0.0.0
CLI_PORT=31800
# Node Identity (from base_configs.env, can override here)
NODE_NAME=edgelake-operator2

# You can override any config from base_configs.env or advance_configs.env here
# Example overrides:
# OVERLAY_IP=<your-ip>
# COMPANY_NAME="My Company"
# CLUSTER_NAME=my-cluster
